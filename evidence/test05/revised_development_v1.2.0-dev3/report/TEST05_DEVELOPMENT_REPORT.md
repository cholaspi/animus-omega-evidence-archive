# Animus Omega Test 05 — Revised Development Report

**Protocol version:** 1.2.0-dev3
**Run class:** `revised_development`
**Development status:** `development` (never `accepted`, `confirmed`, or `published`)
**Repository / branch:** `cholaspi/animus-omega-evidence-archive` / `claude/test-05-animus-omega-dev-oyb6kq`
**Frozen source commit:** `d2a5240b3c5e41deb41fa4242036123e8e707650`
**Canonical evidence path:** `evidence/test05/revised_development_v1.2.0-dev3/`
**Exact pilot superseded:** `evidence/test05/superseded_development_pilot/`

This report is generated by a human/agent reading validated evidence after the
fact. It is descriptive only — no field in it is trusted in place of the
canonical JSON evidence it summarizes, and the independent validator's
determination (`results/integrated_determination.json`) is authoritative for
the integrated status, not this document.

---

## 1. Provenance and freeze integrity

| Item | Value |
|---|---|
| Frozen source commit | `d2a5240b3c5e41deb41fa4242036123e8e707650` |
| Source hash (16 files under `src/animus_test05/`) | `16fe04ba3b2a1387df5ff0c1afe9840b1a08c001151ba79ac0bb3533bb4c84b5` |
| Protocol hash (`TEST_05_PROTOCOL_v1.2.0-dev3.md`) | `9fd636c0fe0e6693c7db8b7abac31ab462a78425e4b01e450c6e6c665695a2eb` |
| Configuration hash | `2ab6109d585611f5db27becc4ef8310ae25cf0ce6f3a2675b000eb02d8e0ccfd` |
| Canonical evidence digest | `360dc89178d2a338059263e37367514d47510e902ef86dbd2ad1ef489d8feaaf` |
| Companion file hashes recorded | 16 |
| Evidence manifest entries | 14 |
| Execution timestamp (noncanonical; excluded from the digest) | `2026-09-11T06:59:48.504782+00:00` |

**Pilot status:** the prior development pilot was sealed and labeled
`superseded_development_pilot` (reason: "protocol improvements were specified
before accepting its conclusions"), never modified, and never had new
acceptance criteria retroactively applied to it. Its provenance (source
commit `fbcd06067695c98a817e0fadff4b1c56eeb632fb`, source hash
`0d95e08024fab159f28ae06277f2ca93c736eed728351eadf0a906c86b0cea42`) was
independently re-derived from `git show <commit>:<path>` bytes before this
run, confirming the pilot's exact source remains recoverable.

**Freeze checklist:** all items independent of test execution
(`pilot_preserved_and_identified`, `companion_files_frozen_and_hashed`,
`all_required_companion_files_present`, `seed_list_fixed_and_checked`,
`simulator_receives_no_report_labels`, `revised_evidence_directory_contains_no_execution_evidence`,
`confirmation_path_disabled`) were confirmed `true` via `freeze_checklist()`
against this exact target directory immediately after `freeze()` and before
`execute()` ran. The two test-result items are externally confirmed here
(not by the checklist function itself, which cannot safely re-run its own
enclosing test suite): **124/124 fast unit tests pass** and **24/24 validator
corruption tests pass** against the frozen commit above (see §5).

**No protocol, configuration, threshold, probe, or seed change occurred
between `freeze()` and `execute()`.** `execute()` and `freeze()` are enforced
as two structurally separate phases: `execute()` raises `RuntimeError` if
`freeze()` has not run, and `freeze()` raises `RuntimeError` if `runs/` or
`results/` are already populated — both guarantees are covered by dedicated
unit tests.

## 2. Seeds

| Family | Development seeds used | Reserved confirmatory seeds used |
|---|---|---|
| friendly | 13001, 13002, 13003 | **No** |
| adversarial | 14001, 14002, 14003 | **No** |

`reserved_seeds_used: false` is recorded directly in the canonical result.
The reserved confirmatory seed bands remain completely untouched — no code
path in this run reads or references them, and `seeds.py`'s
`confirm_reserved_execution()` gate (which would be required to run them)
was never invoked.

## 3. World families evaluated

Exactly 2 world families, each with its own standard and observer
configuration (never described as more than 2 independent families from one
template):

- **friendly** — 3 agents, 2 obligations, no adversarial fault classes declared.
- **adversarial** — 3 agents, 3 obligations, adversarial fault classes
  `identity_substitution` and `causal_reorder` declared.

Both families were exactly enumerated (no sampling) — 256 admissible
microstates and 256 histories per standard configuration, 16,384 histories
per observer configuration, for a total of **33,280 exhaustively enumerated
histories** and **512 exhaustively enumerated microstates** across both
families and both configuration types.

## 4. Execution matrix (fault injection)

| Family | Executions | All predictions matched? | Unexpected predictions |
|---|---|---|---|
| friendly | 3 (baseline only; no adversarial mutations declared) | Yes | — |
| adversarial | 30 (3 seeds × 10 executions: unfaulted + 9 named mutations) | **No** | `adversarial-seed14001-causal_reorder`, `adversarial-seed14003-causal_reorder` |

Each execution recorded `seed`, `execution_id`, `mutation_id`,
`before_hash`, `after_hash`, `mutated_fields`, `predicted_failure`, and
`observed_failure`. 2 of 30 adversarial executions (both `causal_reorder`)
did not match the general fault prediction, even after the reorder mutation
was strengthened from a blind list reversal to a search for a genuinely
causally-dependent pair of `resolve` events to swap. This is preserved as a
genuine, informative finding rather than adjusted after the fact — per the
governing constraint that a failed or partially-failed development result
must be preserved, not engineered away.

## 5. Automated and corruption test results

- **Fast unit tests:** 124/124 pass (`test_world`, `test_boundary`,
  `test_residual`, `test_observer`, `test_resource`, `test_integrated`,
  `test_seeds_hashing`, `test_expansion`, `test_execution_matrix`, `test_run`).
- **Validator corruption tests:** 24/24 pass (`test_validator_corruption.py`),
  covering every rejection class in the protocol's §18 list, including a
  full `freeze()` + `execute()` + `validate()` baseline round-trip.
- Both suites were run against the exact frozen commit
  `d2a5240b3c5e41deb41fa4242036123e8e707650` before tick zero.

## 6. Component results by world family

| Component | friendly | adversarial |
|---|---|---|
| reciprocal_closure | supported | supported |
| loss | supported | supported |
| semantic | supported | supported |
| execution_matrix | supported | **unsupported** |
| observer | **unsupported** | **unsupported** |
| resource | **unsupported** | **unsupported** |
| expansion_contraction | **unsupported** | **unsupported** |

### 6.1 Reciprocal closure (05A)

**Status: supported.** At least one world family exhibits both closing and
non-closing natural histories; all disruptive interventions break closure;
the relevant/irrelevant causal-path distinction holds; ignored-return-value
detection works. The `FrozenContract` is computed and hashed from
`WorldConfig` alone before any history executes, and `execute_J()` both
calls and marks-consumed the frozen contract in every arm of every
evaluated world family (verified, not just asserted — `contract.consumed`
is checked directly).

### 6.2 Genuine information loss (05B)

**Status: supported** for both families — each family's residual has at
least one collision group (two distinct admissible microstates mapping to
the same residual).

### 6.3 Semantic continuity (05B)

**Status: supported** for both families — every core probe (9/9) and every
delayed probe (8/8, generated post-dump from a frozen, hashed generator
function) passes; positive controls pass; negative controls correctly fail;
the leakage audit (`label_audit.clean: true`, 9 simulator-core functions
checked, 0 violations) is clean.

### 6.4 Ledger causality (05B/05A)

**Status: supported.** Removing, staling, or corrupting the ledger breaks
semantic probes in both families (05B), and corrupting the ledger breaks
closure via 05A arm 8 in both families.

### 6.5 Observer boundary detection (05C)

**Status: unsupported** for both families under the dual threshold
(TV distance ≤ 0.05 **and** Bayes-optimal accuracy ≤ 0.55, computed by exact
enumeration, not sampling — 100% matching coverage in both families):

| Family | Primary observer TV distance | Bayes-optimal accuracy | Matching coverage |
|---|---|---|---|
| friendly | 0.276306 | 0.638153 | 1.0 |
| adversarial | 0.240234 | 0.620117 | 1.0 |

The primary observer (radius-one, 2-tick memory, no clock/boundary-marker/
full-state/ledger access) can distinguish interior from boundary windows
well above the required indistinguishability threshold in both families —
i.e. the bounded observer in this model **can** tell where the world's
boundary was, which does not support the required property.

**Positive control:** valid in both families (full-state observer:
TV = 1.0, accuracy = 1.0, comfortably above the 0.25 / 0.75 validity
thresholds), confirming the detection methodology itself is sound.

**Sensitivity ladder** (6 rungs; 5 computed, 1 marked inconclusive by
design):

| Observer | friendly TV | friendly acc. | adversarial TV | adversarial acc. |
|---|---|---|---|---|
| radius_one_local | 0.211 | 0.606 | 0.182 | 0.591 |
| local_short_memory | 0.232 | 0.616 | 0.238 | 0.619 |
| local_relationship_memory | 0.232 | 0.616 | 0.238 | 0.619 |
| local_obligation_history | 0.276 | 0.638 | 0.240 | 0.620 |
| bounded_longer_memory | 0.296 | 0.648 | 0.253 | 0.627 |
| primary_with_eight_tick_memory | — | — | — | — |

`primary_with_eight_tick_memory` is reported **inconclusive** by design:
exact enumeration at an 8-tick memory window exceeds the protocol's
1,000,000-history exhaustive-enumeration bound, and the protocol requires
marking this rung inconclusive rather than silently substituting sampling.
The exact arithmetic is recorded in the component evidence.

### 6.6 Fidelity-matched resource comparison (05D)

**Status: unsupported** for both families under the strict rule (the
proposed residual+ledger architecture must beat **every** eligible baseline
on peak canonical bytes — byte-ticks and op-count are diagnostic only and
cannot rescue a primary-metric loss). Both families show the same 3
regression arms:

- `general_purpose_lossless_compression`
- `general_purpose_lossy_compression_matched_fidelity`
- `scripted_cyclic_replay`

All 10 resource categories are declared for every arm (never defaulted to
zero), and 9 architectures were eligible for comparison in both families.

### 6.7 Expansion/contraction requirement

**Status: unsupported** for both families — proven mathematically
impossible at these world sizes, not merely unobserved in the histories
checked (all 256 admissible histories per family were exhaustively checked):

| Family | R(genesis) | Required for 2× expansion | Provable ceiling |
|---|---|---|---|
| friendly | 3 | 6 | 5 |
| adversarial | 4 | 8 | 6 |

R(t) is defined as (agents holding nonzero balance) + (obligations still
open). No admissible history in either family can reach the required R(t1)
≥ 2·R(t0) threshold given each family's provable ceiling.

## 7. Integrated result

**The generator never asserts a supported/not-supported integrated status.**
`results/test05_development_result.json`'s `integrated_result.status` is
written as literal `"pending"`; the 13 gate evaluations are stored only as
`precomputed_gates_for_validator_use`, for the independent validator's own
use, not as an assertion.

The independent validator (`validator.py`) recomputed all 13 gates from raw
component evidence and wrote the authoritative
`results/integrated_determination.json`:

```
status: not_supported
failed_gates:
  - no_qualifying_distinction_for_primary_bounded_observer
  - fidelity_matched_resource_advantage
inconclusive_gates: []
ineligible_gates: []
invalid_gates: []
```

**Interpretation (verbatim from the evidence):** "At least one integration
gate did not pass ... The complete Animus Omega conjecture is therefore not
supported by this development run. This is not evidence that it is
disproved: it means this bounded model and protocol did not jointly
demonstrate every required property in the same run. Every component result
above remains valid and preserved regardless of this integrated outcome."

## 8. Independent validator result

`validator/validation_report.json`: **57 checks run, 0 violations, `valid: true`.**

This means the evidence is internally consistent under full recomputation
from raw data (hashes match, history/microstate/collision counts recompute
correctly, no probe-answer leakage, no label-driven simulator behavior,
observer distributions and matching coverage recompute correctly, resource
eligibility and peak-byte totals recompute correctly, and the stored
`integrated_gates.json` matches a fresh recomputation of `integrated.evaluate()`).
**A clean validator result is a claim about evidence integrity, not about
whether the conjecture was supported** — the conjecture was not supported,
per §7 above, on this same clean evidence.

The validator's 24 dedicated corruption fixtures (one per rejection class
enumerated in the protocol's §18) were separately verified in §5 to
correctly produce nonzero-violation rejections; none of those corruption
fixtures were used to produce this real run's evidence.

## 9. Limitations (verbatim from canonical evidence)

- The operational `FrozenContract` carries two fields
  (`total_conserved_invariant`, `min_resolutions`); the fuller section-8
  schema is documented in `protocol/beginning_contract_schema.json` but not
  yet threaded through J's runtime input.
- The "primary observer with eight ticks of memory" sensitivity rung is
  reported inconclusive: exact enumeration at that window size is
  combinatorially infeasible.
- R(t) is defined as (agents holding nonzero balance) + (obligations still
  open); this world model's resource/obligation ceiling makes 2× expansion
  mathematically impossible at the current world sizes.
- The `causal_reorder` fault mutation did not match its general prediction
  for 2 of 6 total executions across both world families.

## 10. Status

- Development status: **development** (not accepted, not confirmed, not published).
- Confirmation was **not** run and no reserved confirmatory seed was touched.
- This artifact is **ready for human review**, and only for human review —
  no further protocol, threshold, or evidence changes have been made after
  tick zero. Any defect discovered from this point forward must be handled
  by preserving this run as invalid or aborted and creating a new protocol
  version, per the protocol's own post-tick-zero rule.
